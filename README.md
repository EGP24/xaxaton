# Система контроля успеваемости и посещаемости студентов

Web-приложение на FastAPI для управления успеваемостью и посещаемостью студентов в университете.

## 🚀 Запуск приложения

Приложение уже запущено и доступно по адресу: **http://localhost:8002**

### Альтернативный запуск

Если нужно перезапустить сервер:

```bash
cd /Users/v.pereverza/лабы/xaxaton
python main.py
```

## 👥 Тестовые учетные данные

### Администратор
- **Логин:** admin
- **Пароль:** admin

### Преподаватель
- **Логин:** teacher
- **Пароль:** teacher

## 📋 Функциональность

### Роль: Администратор

Администратор имеет полный доступ ко всем функциям системы:

1. **Управление группами** - создание, редактирование и удаление учебных групп
2. **Управление дисциплинами** - создание, редактирование и удаление предметов
3. **Управление аудиториями** - создание, редактирование и удаление аудиторий
4. **Управление студентами** - добавление, редактирование и удаление студентов
5. **Управление расписанием** - создание и управление расписанием занятий
6. **Управление пользователями** - создание преподавателей и администраторов
7. **Журнал успеваемости** - отметка оценок и посещаемости для любой группы

### Роль: Преподаватель

Преподаватель может:

1. **Просматривать журнал** - только для своих занятий
2. **Отмечать посещаемость** - для студентов на своих занятиях
3. **Выставлять оценки** - для студентов на своих занятиях

## 📊 Журнал успеваемости

Журнал представлен в виде таблицы:
- **Строки** - студенты группы (ФИО)
- **Столбцы** - даты занятий с типами (Л - лекция, С - семинар, ЛР - лабораторная работа)

Для каждой ячейки доступно:
- ✓ Чекбокс для отметки присутствия
- Поле для ввода оценки (0-100)

### Особенности:

- **Потоковые пары** - когда несколько групп объединяются для одного занятия
- **Множественные занятия** - в одну дату может быть несколько пар разных типов (для каждой создается отдельный столбец)
- **Автосохранение** - изменения сохраняются автоматически при вводе

## 🗂️ Структура проекта

```
xaxaton/
├── main.py              # Главный файл приложения
├── models.py            # Модели базы данных
├── schemas.py           # Pydantic схемы
├── auth.py              # Аутентификация и авторизация
├── database.py          # Настройка БД
├── requirements.txt     # Зависимости
├── university.db        # SQLite база данных (создается автоматически)
├── templates/           # HTML шаблоны
│   ├── login.html      # Страница входа
│   ├── admin.html      # Панель администратора
│   └── journal.html    # Журнал успеваемости
└── static/             # Статические файлы
    └── admin.js        # JavaScript для админ-панели
```

## 🔧 Технологии

- **Backend:** FastAPI, SQLAlchemy, Python-JOSE (JWT)
- **Frontend:** Vanilla JavaScript, HTML5, CSS3
- **База данных:** SQLite
- **Аутентификация:** JWT токены

## 📝 API Endpoints

### Аутентификация
- `POST /token` - Получение JWT токена
- `GET /api/me` - Получение информации о текущем пользователе

### Управление (только для администратора)
- `GET/POST/PUT/DELETE /api/groups` - Группы
- `GET/POST/PUT/DELETE /api/disciplines` - Дисциплины
- `GET/POST/PUT/DELETE /api/classrooms` - Аудитории
- `GET/POST/PUT/DELETE /api/students` - Студенты
- `GET/POST/DELETE /api/schedule` - Расписание
- `GET/POST /api/users` - Пользователи

### Журнал (для преподавателей и администраторов)
- `GET /api/journal` - Получение журнала
- `POST /api/attendance` - Отметка посещаемости
- `POST /api/grades` - Выставление оценок

## 🎯 Примеры использования

### 1. Начало работы (Администратор)

1. Войдите как администратор (admin/admin)
2. Создайте группы студентов
3. Добавьте дисциплины
4. Создайте аудитории
5. Добавьте студентов в группы
6. Создайте расписание занятий
7. Перейдите в журнал для отметки успеваемости

### 2. Работа преподавателя

1. Войдите как преподаватель (teacher/teacher)
2. Выберите группу и дисциплину
3. Укажите период дат
4. Отмечайте посещаемость и выставляйте оценки

### 3. Создание потоковой пары

1. В разделе "Расписание"
2. Поставьте галочку "Потоковая пара"
3. Выберите несколько групп (удерживая Ctrl/Cmd)
4. Сохраните расписание

## 🔐 Безопасность

- Все пароли хэшируются с использованием bcrypt
- Аутентификация через JWT токены
- Разграничение прав доступа (администратор/преподаватель)
- Преподаватели видят только свои занятия

## 📱 Интерфейс

Приложение адаптивно и работает на различных устройствах. Интерфейс выполнен в современном стиле с использованием градиентов и плавных анимаций.

## 🐛 Известные предупреждения

При запуске могут появляться предупреждения о deprecated `on_event`, это не влияет на работу приложения.

## 📄 Лицензия

Учебный проект для хакатона.

